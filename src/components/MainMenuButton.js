import React from 'react';

const MainMenuButton = () => {
   return (
      <div className="main-menu-button">
         <span className="main-menu-button__line" />
      </div>
   );
};

export default MainMenuButton;